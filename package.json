{
  "name": "Chainlink",
  "scripts": {
    "build": "turbo run build",
    "watch": "lerna run --parallel --stream --scope '{chainlink-app,chainlink}' watch"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.7.1",
  "dependencies": {
    "lerna": "^8.1.8"
  }
}
